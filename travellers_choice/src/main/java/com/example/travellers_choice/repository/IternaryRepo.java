package com.example.travellers_choice.repository;

import com.example.travellers_choice.model.Iternary;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IternaryRepo extends JpaRepository<Iternary,Integer> {
}
