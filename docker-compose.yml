services:
  mysql:
    image: mysql:8.0
    container_name: mysql-container
    restart: always
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: Praga@060802
      MYSQL_DATABASE: travellers_choice
      MYSQL_ROOT_HOST: "%"
    ports:
      - "3307:3306"
    volumes:
      - tour-data:/var/lib/mysql
    networks:
      - tour-network
    healthcheck:
      test: ["CMD","mysqladmin","ping","-h","localhost"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  app:
    build: .
    container_name: tour-app
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - tour-network

networks:
  tour-network:
    driver: bridge

volumes:
  tour-data: